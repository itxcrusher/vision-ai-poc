# Model weights (download automatically on first run)
*.pt
*.onnx
*.engine

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# Ultralytics runs output
runs/

# OS
.DS_Store
Thumbs.db

# Client-derived detector (mirrors the private the paired device client detector, carries
# client-specific integration details). This repo is PUBLIC: never commit it here.
detector-standalone/
